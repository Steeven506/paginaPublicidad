---
import Footer from "../components/Footer.astro";
import LoginModal from "../components/LoginModal.astro";
import "../styles/global.css";

interface Props {
    title?: string;
}
const { title = "Astro Basics" } = Astro.props;
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>

        <script
            src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"
            defer></script>
        <style is:global>
            [x-cloak] {
                display: none !important;
            }
        </style>
    </head>

    <body class="min-h-screen flex flex-col bg-gray-50">
        <Headers />
        <button
            @click="$dispatch('open-login')"
            class="ml-4 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded transition-colors"
            x-data="{ tooltip: false }"
            @mouseenter="tooltip = true"
            @mouseleave="tooltip = false"
        >
            Iniciar Sesión
            <span
                x-show="tooltip"
                x-transition
                class="absolute z-10 mt-2 px-2 py-1 text-xs bg-gray-800 text-white rounded"
            >
                Acceder a tu cuenta
            </span>
        </button>
        <main class="flex-grow container mx-auto px-4 py-8">
            <slot />
        </main>

        <LoginModal />

        <Footer />
    </body>
</html>
